n = int(input())
paths = []
for _ in range(n):
    paths.append(input().strip())

# Отсортируем пути лексикографически
paths.sort()

result = []
# Используем множество для быстрого поиска, но нам нужна логика проверки префикса
# Более простой способ:
# 1. Положить все пути в множество для O(1) проверки наличия, если бы нужно было удалять.
# Но нам нужно оставить верхние.

# Подход:
# 1. Отсортировать пути.
# 2. Идти по путям. Если текущий путь не начинается с любого из уже добавленных в результат путей + '/', то добавить его.
# Однако проверка "начинается ли с любого из добавленных" может быть медленной, если путей много.
# Но учитывая ограничения (обычно n <= 10^5), можно оптимизировать.

# Более эффективный подход:
# 1. Отсортировать пути.
# 2. Пройтись по ним. Если текущий путь не является подкаталогом предыдущего *выбранного* пути, то выбрать его.
# Но выбрать нужно так, чтобы если есть /a и /a/b, то /a выбрался бы первым (из-за сортировки /a идет перед /a/b).
# И тогда /a/b должен быть пропущен.

chosen_paths = []

for p in paths:
    # Проверяем, не является ли текущий путь p подкаталогом какого-либо из уже выбранных путей.
    # Так как пути отсортированы, нам достаточно проверить, не начинается ли p с последнего добавленного выбранного пути?
    # Нет, потому что сортировка лексикографическая: /a, /a/b, /b, /b/c.
    # После выбора /a, следующим может быть /a/b. Он должен быть пропущен.
    # После выбора /a, следующим может быть /b. Он должен быть выбран.
    # Лексикографически /a < /a/b < /b.
    # Значит, если текущий путь начинается с предыдущего выбранного пути + '/', значит он подкаталог и его пропускаем.
    # А если нет, то добавляем в список выбранных.

    # Важно: нужно проверять не только с последним добавленным, а с тем, который мог быть префиксом.
    # Благодаря сортировке, если p имеет префикс, то этот префикс будет перед p.
    # И если этот префикс был выбран, то он был добавлен в результат.
    # Но какой именно? Самый близкий к p по лексикографическому порядку, который является префиксом, будет сразу перед p или недалеко перед ним.
    # На самом деле, если мы отсортировали пути, то любой префикс пути p будет идти перед p.
    # Если мы сохраняем все "верхние" пути в список result, то для проверки, является ли p подкаталогом любого из них,
    # можно было бы использовать trie или просто проверять со всеми, но это медленно.

    # Оптимизация:
    # Так как мы проходим по отсортированным путям, то если p является подкаталогом какого-то пути q,
    # то q < p. И более того, q будет идти непосредственно перед p в отсортированном списке,
    # если q является максимальным по лексикографическому порядку префиксом среди всех найденных до сих пор?
    # Не совсем. Пример: /a, /a/b, /a/b/c, /b.
    # Сортировка: /a, /a/b, /a/b/c, /b.
    # 1. Берем /a. Результат: [/a].
    # 2. Берем /a/b. Начинается с /a/? Да. Пропускаем.
    # 3. Берем /a/b/c. Начинается с /a/? Да. Пропускаем. (Или с /a/b? Но /a/b не выбран).
    #    Здесь важно: мы должны проверять против ВСЕХ выбранных путей, а не только последнего.
    #    Но так как /a выбран, а /a/b/c начинается с /a, мы его пропустим.
    # 4. Берем /b. Начинается с /a? Нет. Результат: [/a, /b].

    # Проблема: если у нас есть /a и /ab.
    # Сортировка: /a, /ab.
    # 1. /a выбран.
    # 2. /ab. Начинается с /a? Да, если проверять просто starts_with. Но /ab НЕ является подкаталогом /a.
    # Подкаталог /a начинается с /a/ (слэш после).
    # Значит, нужно проверять starts_with(selected_path + '/').

    # Чтобы не проверять со всеми выбранными (это O(N^2) в худшем случае), воспользуемся тем, что сортировка помогает.
    # Если мы добавляем путь в результат, то мы можем забыть про него для последующих проверок? Нет.
    # Но заметим: если p начинается с q + '/', то q < p.
    # И если q было выбрано, то p будет пропущен.
    # Какой q может быть таким? Любой выбранный путь, являющийся префиксом.
    # Но благодаря сортировке, если есть такой q, то он будет "близко" к p.
    # На самом деле, достаточно проверить последний добавленный путь?
    # Рассмотрим: /a, /a/b, /b.
    # Выбраны: /a. Далее /a/b. Пропуск. Далее /b. Проверка с /a? /b не начинается с /a/. OK.
    # Рассмотрим: /a, /b, /b/c, /b/c/d.
    # Выбран /a.
    # Далее /b. Не начинается с /a/. Добавляем /b.
    # Далее /b/c. Начинается с /b/? Да. Пропуск.
    # Далее /b/c/d. Начинается с /b/? Да. Пропуск.

    # А что если: /a/b, /a.
    # Сортировка: /a, /a/b.
    # Выбран /a.
    # Далее /a/b. Пропуск.

    # А что если: /a, /a/c, /a/b.
    # Сортировка: /a, /a/b, /a/c.
    # Выбран /a.
    # /a/b: starts with /a/? Да. Пропуск.
    # /a/c: starts with /a/? Да. Пропуск.

    # Казалось бы, достаточно проверять с ПОСЛЕДНИМ добавленным в результат путем?
    # Проверим контрпример:
    # Пути: /a, /b, /a/b.
    # Сортировка: /a, /a/b, /b.
    # 1. Результат: [/a].
    # 2. Следующий /a/b. Начинается с /a/? Да. Пропуск.
    # 3. Следующий /b. Начинается с /a/? Нет. Добавляем /b. Результат: [/a, /b].
    # Все верно.

    # Еще контрпример:
    # Пути: /a/b, /b/c. (Нет верхних).
    # Сортировка: /a/b, /b/c.
    # 1. Результат: [/a/b].
    # 2. /b/c. Начинается с /a/b/? Нет. Добавляем. Результат: [/a/b, /b/c].
    # Верно.

    # А если: /a, /a/b, /c, /c/d.
    # Сортировка: /a, /a/b, /c, /c/d.
    # 1. /a -> res=[/a]
    # 2. /a/b -> starts with /a/? Yes -> skip
    # 3. /c -> starts with /a/? No -> res=[/a, /c]
    # 4. /c/d -> starts with /a/? No. Но должно начинаться с /c/?
    #    Если мы проверяем только с последним добавлением (/c), то /c/d начнется с /c/. Все верно.

    # Но что если: /a, /b, /a/b.
    # Сортировка: /a, /a/b, /b.
    # 1. /a -> res=[/a]
    # 2. /a/b -> starts with /a/? Yes -> skip
    # 3. /b -> starts with /a/? No -> res=[/a, /b]
    # Все ок.

    # Проблема возникает, если мы пропускаем элементы, и следующий выбираемый элемент не является "близким" префиксом для последующих?
    # Нет, потому что сортировка гарантирует, что если p является подкаталогом q, то q идет раньше p.
    # И если q был выбран, то он добавлен в res.
    # Но мы проверяем только с ПОСЛЕДНИМ добавленным в res.
    # Если p начинается с q + '/', а q не является последним добавленным, а был добавлен раньше, то между q и p в отсортированном списке могут быть другие пути, которые были пропущены (потому что являются подкаталогами q или чего-то еще).
    # Например: /a, /a/b, /a/b/c, /b.
    # Сортировка: /a, /a/b, /a/b/c, /b.
    # 1. res=[/a].
    # 2. /a/b. starts with /a/? Yes. Skip.
    # 3. /a/b/c. starts with /a/? No (проверяем с последним добавленным /a).
    #    Wait! Если мы проверяем только с последним *выбранным* (/a), то /a/b/c starts with /a/? Yes. So skip.
    #    Это работает, потому что /a является префиксом для всех вложенных.

    # А если: /a/b, /a.
    # Сортировка: /a, /a/b.
    # 1. res=[/a].
    # 2. /a/b. starts with /a/? Yes. Skip.

    # Кажется, проверка с последним добавленным в результат путем достаточно, потому что:
    # Если p начинается с q + '/', то q < p.
    # Все пути между q и p в отсортированном списке либо являются подкаталогами q (и будут пропущены), либо начинаются с чего-то другого.
    # Если есть путь r между q и p, который не является подкаталогом q, то r < p.
    # Если r < p и r не является подкаталогом q, то r может быть префиксом для p?
    # Если r является префиксом для p, то r должен быть добавлен в res, если он не является подкаталогом чего-то еще.
    # И так как r > q (lexicographically, usually), то r был бы добавлен *после* q.
    # Но если r был добавлен после q, то он и будет "последним добавленным" на момент проверки p.
    # Значит, мы будем проверять p с r, а не с q.
    # Если p начинается с r + '/', то мы его пропустим.
    # Если нет, то мы проверяем с r. Но p может начинаться с q + '/'.
    # Если p начинается с q + '/', но не с r + '/', это возможно только если q является префиксом r, но r не добавлен?
    # Если q является префиксом r, то r является подкаталогом q.
    # Но тогда r был бы пропущен при обработке r!
    # Т.е. если q выбран, то все подкаталоги q (включая r) будут пропущены.
    # Значит, между q и p не может быть выбранного пути r, который не является подкаталогом q.
    # Следовательно, последний выбранный путь перед p (если он есть) должен быть либо q, либо кем-то, что не связано с q.
    # Если между q и p нет выбранных путей, то последний выбранный — это q (или тот, что перед q).
    # Если последний выбранный — это q, то мы проверяем p с q. Если p начинается с q + '/', то пропуск.
    # Если последний выбранный — это q' перед q, то q был выбран. Значит p должен был быть проверен с q?
    # Нет, мы проверяем с *последним добавленным*.
    # Порядок: q добавляется. Затем идут пропущенные (подкаталоги q). Затем p.
    # Последний добавленный все еще q.
    # Значит мы проверяем p с q. И если p подкаталог q, то пропускаем.

    # Вывод: проверка только с последним добавленным в результат путем корректна при сортировке.

    if chosen_paths:
        last_chosen = chosen_paths[-1]
        if p.startswith(last_chosen + '/'):
            continue

    chosen_paths.append(p)

for p in chosen_paths:
    print(p)